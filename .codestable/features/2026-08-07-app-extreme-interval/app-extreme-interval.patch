diff --git a/front/app/liepin/page.tsx b/front/app/liepin/page.tsx
index 77c18cb..546d0f1 100644
--- a/front/app/liepin/page.tsx
+++ b/front/app/liepin/page.tsx
@@ -133,17 +133,17 @@ export default function LiepinPage() {
     aiReviewMinScore: 60,
     aiBatchSize: 5,
     maxPerRun: 10,
-    minDelaySeconds: 90,
-    maxDelaySeconds: 180,
-    searchMinDelaySeconds: 30,
-    searchMaxDelaySeconds: 60,
-    pageMinDelaySeconds: 15,
-    pageMaxDelaySeconds: 30,
-    detailMinDelaySeconds: 20,
-    detailMaxDelaySeconds: 40,
+    minDelaySeconds: 30,
+    maxDelaySeconds: 60,
+    searchMinDelaySeconds: 10,
+    searchMaxDelaySeconds: 20,
+    pageMinDelaySeconds: 5,
+    pageMaxDelaySeconds: 10,
+    detailMinDelaySeconds: 8,
+    detailMaxDelaySeconds: 15,
     rateGuardBatchSize: 5,
-    batchCooldownMinSeconds: 600,
-    batchCooldownMaxSeconds: 900,
+    batchCooldownMinSeconds: 300,
+    batchCooldownMaxSeconds: 450,
   })
   const [options, setOptions] = useState<LiepinOptions>({
     city: [],
@@ -285,21 +285,21 @@ export default function LiepinPage() {
         normalized.aiReviewMinScore = Math.max(0, Math.min(normalized.aiMinScore, data.config.aiReviewMinScore ?? 60))
         normalized.aiBatchSize = Math.max(3, Math.min(10, data.config.aiBatchSize ?? 5))
         normalized.maxPerRun = Math.min(data.config.maxPerRun ?? 10, 10)
-        normalized.minDelaySeconds = Math.max(data.config.minDelaySeconds ?? 90, 90)
-        normalized.maxDelaySeconds = Math.max(data.config.maxDelaySeconds ?? 180, normalized.minDelaySeconds, 180)
-        normalized.searchMinDelaySeconds = Math.max(15, Math.min(300, data.config.searchMinDelaySeconds ?? 30))
+        normalized.minDelaySeconds = Math.max(data.config.minDelaySeconds ?? 30, 30)
+        normalized.maxDelaySeconds = Math.max(data.config.maxDelaySeconds ?? 60, normalized.minDelaySeconds, 60)
+        normalized.searchMinDelaySeconds = Math.max(5, Math.min(300, data.config.searchMinDelaySeconds ?? 10))
         normalized.searchMaxDelaySeconds = Math.max(normalized.searchMinDelaySeconds,
-          Math.min(300, Math.max(30, data.config.searchMaxDelaySeconds ?? 60)))
-        normalized.pageMinDelaySeconds = Math.max(15, Math.min(300, data.config.pageMinDelaySeconds ?? 15))
+          Math.min(300, Math.max(10, data.config.searchMaxDelaySeconds ?? 20)))
+        normalized.pageMinDelaySeconds = Math.max(5, Math.min(300, data.config.pageMinDelaySeconds ?? 5))
         normalized.pageMaxDelaySeconds = Math.max(normalized.pageMinDelaySeconds,
-          Math.min(300, Math.max(15, data.config.pageMaxDelaySeconds ?? 30)))
-        normalized.detailMinDelaySeconds = Math.max(15, Math.min(300, data.config.detailMinDelaySeconds ?? 20))
+          Math.min(300, Math.max(5, data.config.pageMaxDelaySeconds ?? 10)))
+        normalized.detailMinDelaySeconds = Math.max(5, Math.min(300, data.config.detailMinDelaySeconds ?? 8))
         normalized.detailMaxDelaySeconds = Math.max(normalized.detailMinDelaySeconds,
-          Math.min(300, Math.max(20, data.config.detailMaxDelaySeconds ?? 40)))
+          Math.min(300, Math.max(8, data.config.detailMaxDelaySeconds ?? 15)))
         normalized.rateGuardBatchSize = Math.max(1, Math.min(10, data.config.rateGuardBatchSize ?? 5))
-        normalized.batchCooldownMinSeconds = Math.max(300, Math.min(3600, data.config.batchCooldownMinSeconds ?? 600))
+        normalized.batchCooldownMinSeconds = Math.max(300, Math.min(3600, data.config.batchCooldownMinSeconds ?? 300))
         normalized.batchCooldownMaxSeconds = Math.max(normalized.batchCooldownMinSeconds,
-          Math.min(3600, data.config.batchCooldownMaxSeconds ?? 900))
+          Math.min(3600, Math.max(300, data.config.batchCooldownMaxSeconds ?? 450)))
         setConfig(normalized)
         // 检查当前城市是否在选项列表中
         if (data.options?.city && data.config.city) {
@@ -919,9 +919,9 @@ export default function LiepinPage() {
                 <Input
                   id="minDelaySeconds"
                   type="number"
-                  min={90}
+                  min={30}
                   max={300}
-                  value={config.minDelaySeconds ?? 90}
+                  value={config.minDelaySeconds ?? 30}
                   onChange={(e) => setConfig({ ...config, minDelaySeconds: Number(e.target.value) })}
                 />
               </div>
@@ -930,9 +930,9 @@ export default function LiepinPage() {
                 <Input
                   id="maxDelaySeconds"
                   type="number"
-                  min={90}
+                  min={30}
                   max={300}
-                  value={config.maxDelaySeconds ?? 180}
+                  value={config.maxDelaySeconds ?? 60}
                   onChange={(e) => setConfig({ ...config, maxDelaySeconds: Number(e.target.value) })}
                 />
               </div>
@@ -941,9 +941,9 @@ export default function LiepinPage() {
                 <Input
                   id="searchMinDelaySeconds"
                   type="number"
-                  min={15}
+                  min={5}
                   max={300}
-                  value={config.searchMinDelaySeconds ?? 30}
+                  value={config.searchMinDelaySeconds ?? 10}
                   onChange={(e) => setConfig({ ...config, searchMinDelaySeconds: Number(e.target.value) })}
                 />
               </div>
@@ -952,9 +952,9 @@ export default function LiepinPage() {
                 <Input
                   id="searchMaxDelaySeconds"
                   type="number"
-                  min={15}
+                  min={5}
                   max={300}
-                  value={config.searchMaxDelaySeconds ?? 60}
+                  value={config.searchMaxDelaySeconds ?? 20}
                   onChange={(e) => setConfig({ ...config, searchMaxDelaySeconds: Number(e.target.value) })}
                 />
               </div>
@@ -963,9 +963,9 @@ export default function LiepinPage() {
                 <Input
                   id="pageMinDelaySeconds"
                   type="number"
-                  min={15}
+                  min={5}
                   max={300}
-                  value={config.pageMinDelaySeconds ?? 15}
+                  value={config.pageMinDelaySeconds ?? 5}
                   onChange={(e) => setConfig({ ...config, pageMinDelaySeconds: Number(e.target.value) })}
                 />
               </div>
@@ -974,9 +974,9 @@ export default function LiepinPage() {
                 <Input
                   id="pageMaxDelaySeconds"
                   type="number"
-                  min={15}
+                  min={5}
                   max={300}
-                  value={config.pageMaxDelaySeconds ?? 30}
+                  value={config.pageMaxDelaySeconds ?? 10}
                   onChange={(e) => setConfig({ ...config, pageMaxDelaySeconds: Number(e.target.value) })}
                 />
               </div>
@@ -985,9 +985,9 @@ export default function LiepinPage() {
                 <Input
                   id="detailMinDelaySeconds"
                   type="number"
-                  min={15}
+                  min={5}
                   max={300}
-                  value={config.detailMinDelaySeconds ?? 20}
+                  value={config.detailMinDelaySeconds ?? 8}
                   onChange={(e) => setConfig({ ...config, detailMinDelaySeconds: Number(e.target.value) })}
                 />
               </div>
@@ -996,9 +996,9 @@ export default function LiepinPage() {
                 <Input
                   id="detailMaxDelaySeconds"
                   type="number"
-                  min={15}
+                  min={5}
                   max={300}
-                  value={config.detailMaxDelaySeconds ?? 40}
+                  value={config.detailMaxDelaySeconds ?? 15}
                   onChange={(e) => setConfig({ ...config, detailMaxDelaySeconds: Number(e.target.value) })}
                 />
               </div>
@@ -1020,7 +1020,7 @@ export default function LiepinPage() {
                   type="number"
                   min={300}
                   max={3600}
-                  value={config.batchCooldownMinSeconds ?? 600}
+                  value={config.batchCooldownMinSeconds ?? 300}
                   onChange={(e) => setConfig({ ...config, batchCooldownMinSeconds: Number(e.target.value) })}
                 />
               </div>
@@ -1031,13 +1031,13 @@ export default function LiepinPage() {
                   type="number"
                   min={300}
                   max={3600}
-                  value={config.batchCooldownMaxSeconds ?? 900}
+                  value={config.batchCooldownMaxSeconds ?? 450}
                   onChange={(e) => setConfig({ ...config, batchCooldownMaxSeconds: Number(e.target.value) })}
                 />
               </div>
             </div>
             <p className="mt-3 text-xs text-muted-foreground">
-              当前提速基线：搜索 30-60 秒、翻页 15-30 秒、详情 20-40 秒、发送 90-180 秒；命中平台风控信号后任务会停止并记录原因。
+              当前极限提速基线：搜索 10-20 秒、翻页 5-10 秒、详情 8-15 秒、发送 30-60 秒；命中平台风控信号后任务会停止并记录原因。
             </p>
             <div className="mt-6 space-y-3 rounded-md border p-4">
               <div className="flex flex-wrap items-center justify-between gap-3">

diff --git a/src/main/java/com/getjobs/application/service/LiepinService.java b/src/main/java/com/getjobs/application/service/LiepinService.java
index add740e..48b2d79 100644
--- a/src/main/java/com/getjobs/application/service/LiepinService.java
+++ b/src/main/java/com/getjobs/application/service/LiepinService.java
@@ -95,37 +95,37 @@ public class LiepinService {
                 stmt.execute("ALTER TABLE liepin_config ADD COLUMN max_per_run INTEGER DEFAULT 10");
             } catch (Exception ignored) {}
             try {
-                stmt.execute("ALTER TABLE liepin_config ADD COLUMN min_delay_seconds INTEGER DEFAULT 90");
+                stmt.execute("ALTER TABLE liepin_config ADD COLUMN min_delay_seconds INTEGER DEFAULT 30");
             } catch (Exception ignored) {}
             try {
-                stmt.execute("ALTER TABLE liepin_config ADD COLUMN max_delay_seconds INTEGER DEFAULT 180");
+                stmt.execute("ALTER TABLE liepin_config ADD COLUMN max_delay_seconds INTEGER DEFAULT 60");
             } catch (Exception ignored) {}
             try {
-                stmt.execute("ALTER TABLE liepin_config ADD COLUMN search_min_delay_seconds INTEGER DEFAULT 30");
+                stmt.execute("ALTER TABLE liepin_config ADD COLUMN search_min_delay_seconds INTEGER DEFAULT 10");
             } catch (Exception ignored) {}
             try {
-                stmt.execute("ALTER TABLE liepin_config ADD COLUMN search_max_delay_seconds INTEGER DEFAULT 60");
+                stmt.execute("ALTER TABLE liepin_config ADD COLUMN search_max_delay_seconds INTEGER DEFAULT 20");
             } catch (Exception ignored) {}
             try {
-                stmt.execute("ALTER TABLE liepin_config ADD COLUMN page_min_delay_seconds INTEGER DEFAULT 15");
+                stmt.execute("ALTER TABLE liepin_config ADD COLUMN page_min_delay_seconds INTEGER DEFAULT 5");
             } catch (Exception ignored) {}
             try {
-                stmt.execute("ALTER TABLE liepin_config ADD COLUMN page_max_delay_seconds INTEGER DEFAULT 30");
+                stmt.execute("ALTER TABLE liepin_config ADD COLUMN page_max_delay_seconds INTEGER DEFAULT 10");
             } catch (Exception ignored) {}
             try {
-                stmt.execute("ALTER TABLE liepin_config ADD COLUMN detail_min_delay_seconds INTEGER DEFAULT 20");
+                stmt.execute("ALTER TABLE liepin_config ADD COLUMN detail_min_delay_seconds INTEGER DEFAULT 8");
             } catch (Exception ignored) {}
             try {
-                stmt.execute("ALTER TABLE liepin_config ADD COLUMN detail_max_delay_seconds INTEGER DEFAULT 40");
+                stmt.execute("ALTER TABLE liepin_config ADD COLUMN detail_max_delay_seconds INTEGER DEFAULT 15");
             } catch (Exception ignored) {}
             try {
                 stmt.execute("ALTER TABLE liepin_config ADD COLUMN rate_guard_batch_size INTEGER DEFAULT 5");
             } catch (Exception ignored) {}
             try {
-                stmt.execute("ALTER TABLE liepin_config ADD COLUMN batch_cooldown_min_seconds INTEGER DEFAULT 600");
+                stmt.execute("ALTER TABLE liepin_config ADD COLUMN batch_cooldown_min_seconds INTEGER DEFAULT 300");
             } catch (Exception ignored) {}
             try {
-                stmt.execute("ALTER TABLE liepin_config ADD COLUMN batch_cooldown_max_seconds INTEGER DEFAULT 900");
+                stmt.execute("ALTER TABLE liepin_config ADD COLUMN batch_cooldown_max_seconds INTEGER DEFAULT 450");
             } catch (Exception ignored) {}
             // 分页断点续跑进度表
             stmt.execute(
@@ -792,7 +792,7 @@ public class LiepinService {
         if (config.getMinDelaySeconds() < 0
                 || config.getMaxDelaySeconds() < config.getMinDelaySeconds()
                 || config.getMaxDelaySeconds() > LiepinConfig.MAX_RATE_DELAY_SECONDS) {
-            throw new IllegalArgumentException("猎聘发送等待需满足 90<=最小秒数<=最大秒数<=300");
+            throw new IllegalArgumentException("猎聘发送等待需满足 30<=最小秒数<=最大秒数<=300");
         }
     }
 

diff --git a/src/main/java/com/getjobs/worker/liepin/Liepin.java b/src/main/java/com/getjobs/worker/liepin/Liepin.java
index d13c709..be29a4a 100644
--- a/src/main/java/com/getjobs/worker/liepin/Liepin.java
+++ b/src/main/java/com/getjobs/worker/liepin/Liepin.java
@@ -497,7 +497,7 @@ public class Liepin {
         if (minDelay < LiepinConfig.MIN_SAFE_SEND_DELAY_SECONDS
                 || maxDelay < minDelay
                 || maxDelay > LiepinConfig.MAX_RATE_DELAY_SECONDS) {
-            throw new IllegalArgumentException("猎聘发送间隔需满足 90<=最小秒数<=最大秒数<=300");
+            throw new IllegalArgumentException("猎聘发送间隔需满足 30<=最小秒数<=最大秒数<=300");
         }
         if ((config.isAutoAiDeliveryEnabled() || config.isBatchShadowEnabled()) && !config.isAiEnabled()) {
             throw new IllegalArgumentException("开启AI投递模式前，请先开启AI招呼语");

diff --git a/src/main/java/com/getjobs/worker/liepin/LiepinConfig.java b/src/main/java/com/getjobs/worker/liepin/LiepinConfig.java
index bd00b86..0d40066 100644
--- a/src/main/java/com/getjobs/worker/liepin/LiepinConfig.java
+++ b/src/main/java/com/getjobs/worker/liepin/LiepinConfig.java
@@ -12,22 +12,22 @@ import java.util.List;
 public class LiepinConfig {
     public static final int MAX_SAFE_PER_RUN = 10;
     public static final int DEFAULT_MAX_PER_RUN = 10;
-    public static final int MIN_SAFE_SEND_DELAY_SECONDS = 90;
-    public static final int DEFAULT_MIN_DELAY_SECONDS = 90;
-    public static final int DEFAULT_MAX_DELAY_SECONDS = 180;
-    public static final int MIN_RATE_DELAY_SECONDS = 15;
+    public static final int MIN_SAFE_SEND_DELAY_SECONDS = 30;
+    public static final int DEFAULT_MIN_DELAY_SECONDS = 30;
+    public static final int DEFAULT_MAX_DELAY_SECONDS = 60;
+    public static final int MIN_RATE_DELAY_SECONDS = 5;
     public static final int MAX_RATE_DELAY_SECONDS = 300;
-    public static final int DEFAULT_SEARCH_MIN_DELAY_SECONDS = 30;
-    public static final int DEFAULT_SEARCH_MAX_DELAY_SECONDS = 60;
-    public static final int DEFAULT_PAGE_MIN_DELAY_SECONDS = 15;
-    public static final int DEFAULT_PAGE_MAX_DELAY_SECONDS = 30;
-    public static final int DEFAULT_DETAIL_MIN_DELAY_SECONDS = 20;
-    public static final int DEFAULT_DETAIL_MAX_DELAY_SECONDS = 40;
+    public static final int DEFAULT_SEARCH_MIN_DELAY_SECONDS = 10;
+    public static final int DEFAULT_SEARCH_MAX_DELAY_SECONDS = 20;
+    public static final int DEFAULT_PAGE_MIN_DELAY_SECONDS = 5;
+    public static final int DEFAULT_PAGE_MAX_DELAY_SECONDS = 10;
+    public static final int DEFAULT_DETAIL_MIN_DELAY_SECONDS = 8;
+    public static final int DEFAULT_DETAIL_MAX_DELAY_SECONDS = 15;
     public static final int DEFAULT_RATE_GUARD_BATCH_SIZE = 5;
     public static final int MIN_RATE_GUARD_BATCH_SIZE = 1;
     public static final int MAX_RATE_GUARD_BATCH_SIZE = 10;
-    public static final int DEFAULT_BATCH_COOLDOWN_MIN_SECONDS = 600;
-    public static final int DEFAULT_BATCH_COOLDOWN_MAX_SECONDS = 900;
+    public static final int DEFAULT_BATCH_COOLDOWN_MIN_SECONDS = 300;
+    public static final int DEFAULT_BATCH_COOLDOWN_MAX_SECONDS = 450;
     public static final int MIN_BATCH_COOLDOWN_SECONDS = 300;
     public static final int MAX_BATCH_COOLDOWN_SECONDS = 3600;
     public static final int DEFAULT_AI_MIN_SCORE = 70;

diff --git a/src/main/java/com/getjobs/worker/liepin/LiepinRateGuard.java b/src/main/java/com/getjobs/worker/liepin/LiepinRateGuard.java
index 7ddbfaf..09b6932 100644
--- a/src/main/java/com/getjobs/worker/liepin/LiepinRateGuard.java
+++ b/src/main/java/com/getjobs/worker/liepin/LiepinRateGuard.java
@@ -87,12 +87,12 @@ final class LiepinRateGuard {
                 clock,
                 sleeper,
                 randomSeconds,
-                new DelayRange(30, 60),
-                new DelayRange(15, 30),
-                new DelayRange(20, 40),
+                new DelayRange(10, 20),
+                new DelayRange(5, 10),
+                new DelayRange(8, 15),
                 sendRange,
                 5,
-                new DelayRange(1200, 1800)
+                new DelayRange(300, 450)
         );
     }
 

diff --git a/src/test/java/com/getjobs/worker/liepin/LiepinRateGuardTest.java b/src/test/java/com/getjobs/worker/liepin/LiepinRateGuardTest.java
index 21036f9..8ad58e1 100644
--- a/src/test/java/com/getjobs/worker/liepin/LiepinRateGuardTest.java
+++ b/src/test/java/com/getjobs/worker/liepin/LiepinRateGuardTest.java
@@ -23,8 +23,8 @@ class LiepinRateGuardTest {
         assertTrue(guard.before(LiepinRateGuard.Action.SEARCH));
         assertTrue(guard.before(LiepinRateGuard.Action.PAGE));
 
-        assertEquals(15_000L, now.get());
-        assertTrue(messages.contains("节奏控制：翻页开始等待15秒"));
+        assertEquals(5_000L, now.get());
+        assertTrue(messages.contains("节奏控制：翻页开始等待5秒"));
         assertTrue(messages.contains("节奏控制：翻页等待结束，继续处理"));
     }
 
@@ -40,12 +40,12 @@ class LiepinRateGuardTest {
         assertTrue(guard.before(LiepinRateGuard.Action.SEARCH));
         assertTrue(guard.before(LiepinRateGuard.Action.PAGE));
 
-        int startIndex = events.indexOf("message:节奏控制：翻页开始等待15秒");
+        int startIndex = events.indexOf("message:节奏控制：翻页开始等待5秒");
         int endIndex = events.indexOf("message:节奏控制：翻页等待结束，继续处理");
         assertTrue(startIndex >= 0);
         assertTrue(endIndex > startIndex + 1);
         assertTrue(events.get(startIndex + 1).startsWith("sleep:"));
-        assertEquals(15_000L, events.subList(startIndex + 1, endIndex).stream()
+        assertEquals(5_000L, events.subList(startIndex + 1, endIndex).stream()
                 .mapToLong(event -> Long.parseLong(event.substring("sleep:".length())))
                 .sum());
     }
@@ -79,12 +79,12 @@ class LiepinRateGuardTest {
                     now.addAndGet(millis);
                 },
                 (min, max) -> min,
-                new LiepinRateGuard.DelayRange(90, 180)
+                new LiepinRateGuard.DelayRange(30, 60)
         );
 
         assertTrue(guard.before(LiepinRateGuard.Action.SEARCH));
         assertFalse(guard.before(LiepinRateGuard.Action.PAGE));
-        assertTrue(messages.contains("节奏控制：翻页开始等待15秒"));
+        assertTrue(messages.contains("节奏控制：翻页开始等待5秒"));
     }
 
     @Test
@@ -99,7 +99,7 @@ class LiepinRateGuardTest {
         assertEquals(0L, now.get());
 
         assertTrue(guard.afterSuccessfulSend(true));
-        assertEquals(1_200_000L, now.get());
+        assertEquals(300_000L, now.get());
     }
 
     @Test
@@ -135,16 +135,24 @@ class LiepinRateGuardTest {
         config.setMaxDelaySeconds(5);
         config.setSearchMinDelaySeconds(1);
         config.setSearchMaxDelaySeconds(2);
+        config.setPageMinDelaySeconds(1);
+        config.setPageMaxDelaySeconds(2);
+        config.setDetailMinDelaySeconds(1);
+        config.setDetailMaxDelaySeconds(2);
         config.setBatchCooldownMinSeconds(1);
         config.setBatchCooldownMaxSeconds(2);
 
         assertEquals(10, config.effectiveMaxPerRun());
-        assertEquals(90, config.effectiveMinDelaySeconds());
-        assertEquals(180, config.effectiveMaxDelaySeconds());
-        assertEquals(15, config.effectiveSearchMinDelaySeconds());
-        assertEquals(60, config.effectiveSearchMaxDelaySeconds());
+        assertEquals(30, config.effectiveMinDelaySeconds());
+        assertEquals(60, config.effectiveMaxDelaySeconds());
+        assertEquals(5, config.effectiveSearchMinDelaySeconds());
+        assertEquals(20, config.effectiveSearchMaxDelaySeconds());
+        assertEquals(5, config.effectivePageMinDelaySeconds());
+        assertEquals(10, config.effectivePageMaxDelaySeconds());
+        assertEquals(5, config.effectiveDetailMinDelaySeconds());
+        assertEquals(15, config.effectiveDetailMaxDelaySeconds());
         assertEquals(300, config.effectiveBatchCooldownMinSeconds());
-        assertEquals(900, config.effectiveBatchCooldownMaxSeconds());
+        assertEquals(450, config.effectiveBatchCooldownMaxSeconds());
     }
 
     @Test
@@ -205,7 +213,7 @@ class LiepinRateGuardTest {
                 now::get,
                 sleeper,
                 (min, max) -> min,
-                new LiepinRateGuard.DelayRange(90, 180)
+                new LiepinRateGuard.DelayRange(30, 60)
         );
     }
 }

diff --git a/src/test/java/com/getjobs/worker/liepin/LiepinConfigTest.java b/src/test/java/com/getjobs/worker/liepin/LiepinConfigTest.java
index 7a46412..94a0f4c 100644
--- a/src/test/java/com/getjobs/worker/liepin/LiepinConfigTest.java
+++ b/src/test/java/com/getjobs/worker/liepin/LiepinConfigTest.java
@@ -25,6 +25,22 @@ class LiepinConfigTest {
         assertEquals(100, config.effectiveAiMinScore());
     }
 
+    @Test
+    void intervalDefaultsUseExtremeAggressivePolicy() {
+        LiepinConfig config = new LiepinConfig();
+
+        assertEquals(30, config.effectiveMinDelaySeconds());
+        assertEquals(60, config.effectiveMaxDelaySeconds());
+        assertEquals(10, config.effectiveSearchMinDelaySeconds());
+        assertEquals(20, config.effectiveSearchMaxDelaySeconds());
+        assertEquals(5, config.effectivePageMinDelaySeconds());
+        assertEquals(10, config.effectivePageMaxDelaySeconds());
+        assertEquals(8, config.effectiveDetailMinDelaySeconds());
+        assertEquals(15, config.effectiveDetailMaxDelaySeconds());
+        assertEquals(300, config.effectiveBatchCooldownMinSeconds());
+        assertEquals(450, config.effectiveBatchCooldownMaxSeconds());
+    }
+
     @Test
     void legacyAutoFlagStillSelectsSingleMode() {
         LiepinConfig config = new LiepinConfig();

