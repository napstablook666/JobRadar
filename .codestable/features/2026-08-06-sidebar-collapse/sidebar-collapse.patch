diff --git a/front/app/components/ContentArea.tsx b/front/app/components/ContentArea.tsx
index 07cc840..b398fc0 100644
--- a/front/app/components/ContentArea.tsx
+++ b/front/app/components/ContentArea.tsx
@@ -3,7 +3,13 @@ import { usePathname } from 'next/navigation'
 import { ReactNode, useMemo } from 'react'
 import { motion } from 'framer-motion'
 
-export default function ContentArea({ children }: { children: ReactNode }) {
+export default function ContentArea({
+  children,
+  sidebarCollapsed,
+}: {
+  children: ReactNode
+  sidebarCollapsed: boolean
+}) {
   const pathname = usePathname()
 
   const accentClass = useMemo(() => {
@@ -22,7 +28,7 @@ export default function ContentArea({ children }: { children: ReactNode }) {
   }, [pathname])
 
   return (
-    <main className={`flex-1 ml-64 bg-background dark:bg-blacksection content-bg ${accentClass} min-h-screen`}>
+    <main className={`flex-1 ${sidebarCollapsed ? 'ml-20' : 'ml-64'} transition-[margin-left] duration-300 bg-background dark:bg-blacksection content-bg ${accentClass} min-h-screen`}>
       <motion.div
         key={pathname}
         initial={{ opacity: 0, y: 20 }}
@@ -35,4 +41,4 @@ export default function ContentArea({ children }: { children: ReactNode }) {
       </motion.div>
     </main>
   )
-}
\ No newline at end of file
+}
diff --git a/front/app/components/Sidebar.tsx b/front/app/components/Sidebar.tsx
index 95ee56f..d19c520 100644
--- a/front/app/components/Sidebar.tsx
+++ b/front/app/components/Sidebar.tsx
@@ -3,11 +3,17 @@
 import Link from 'next/link'
 import { usePathname } from 'next/navigation'
 import { useEffect, useState } from 'react'
-import { BiEnvelope, BiBriefcase, BiSearch, BiTask, BiUserCircle, BiBrain, BiMoon, BiSun } from 'react-icons/bi'
+import { BiEnvelope, BiBriefcase, BiSearch, BiTask, BiUserCircle, BiBrain, BiMoon, BiSun, BiChevronLeft, BiChevronRight } from 'react-icons/bi'
 import { motion } from 'framer-motion'
 import { useTheme } from 'next-themes'
 
-export default function Sidebar() {
+export default function Sidebar({
+  collapsed,
+  onCollapsedChange,
+}: {
+  collapsed: boolean
+  onCollapsedChange: (collapsed: boolean) => void
+}) {
   const pathname = usePathname()
   const { theme, setTheme } = useTheme()
   const [mounted, setMounted] = useState(false)
@@ -47,7 +53,7 @@ export default function Sidebar() {
         } else {
           setHealth('down')
         }
-      } catch (e) {
+      } catch {
         setHealth('unknown')
       } finally {
         clearTimeout(timeout)
@@ -80,25 +86,25 @@ export default function Sidebar() {
       initial={{ x: -100, opacity: 0 }}
       animate={{ x: 0, opacity: 1 }}
       transition={{ duration: 0.5, ease: "easeOut" }}
-      className="fixed left-0 top-0 h-full w-64 bg-gradient-to-b from-blue-600 via-indigo-600 to-purple-600 dark:from-blacksection dark:via-blackho dark:to-black shadow-solid-8 z-50 border-r border-white/10 dark:border-strokedark"
+      className={`fixed left-0 top-0 z-50 flex h-full ${collapsed ? 'w-20' : 'w-64'} flex-col overflow-hidden border-r border-white/10 bg-gradient-to-b from-blue-600 via-indigo-600 to-purple-600 shadow-solid-8 transition-[width] duration-300 dark:border-strokedark dark:from-blacksection dark:via-blackho dark:to-black`}
     >
       {/* 侧边栏头部 */}
       <motion.div
         initial={{ y: -20, opacity: 0 }}
         animate={{ y: 0, opacity: 1 }}
         transition={{ delay: 0.2, duration: 0.5 }}
-        className="p-6 border-b border-white/20 dark:border-strokedark"
+        className={`shrink-0 border-b border-white/20 transition-[padding] duration-300 dark:border-strokedark ${collapsed ? 'p-3' : 'p-6'}`}
       >
-        <div className="flex items-center gap-3 mb-2">
+        <div className={`mb-2 flex items-center ${collapsed ? 'justify-center' : 'gap-3'}`}>
           <span className="text-4xl leading-none">🍀</span>
-          <div>
+          <div className={collapsed ? 'hidden' : ''}>
             <h1 className="text-xl font-bold text-white">Get Jobs</h1>
             <p className="text-white dark:text-manatee text-sm">配置管理中心</p>
           </div>
         </div>
 
         {/* 状态指示器（动态健康检查） */}
-        <div className="mt-4 flex items-center gap-2 text-white dark:text-manatee text-sm">
+        <div className={`mt-4 flex items-center text-sm text-white dark:text-manatee ${collapsed ? 'justify-center' : 'gap-2'}`}>
           <div
             className={`w-2 h-2 rounded-full animate-pulse ${
               health === 'up'
@@ -110,7 +116,7 @@ export default function Sidebar() {
                 : 'bg-gray-400'
             }`}
           ></div>
-          <span className="text-white dark:text-manatee">
+          <span className={`text-white dark:text-manatee ${collapsed ? 'sr-only' : ''}`}>
             {health === 'up'
               ? '系统运行正常'
               : health === 'degraded'
@@ -128,28 +134,46 @@ export default function Sidebar() {
             animate={{ scale: 1 }}
             transition={{ delay: 0.4, type: "spring", stiffness: 200 }}
             onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')}
-            className="mt-4 w-full flex items-center justify-center gap-2 px-4 py-2.5 rounded-lg bg-white/10 hover:bg-white/20 dark:bg-white/5 dark:hover:bg-white/10 text-white transition-all duration-300 shadow-solid-3"
+            className={`mt-4 flex w-full items-center justify-center rounded-lg bg-white/10 py-2.5 text-white shadow-solid-3 transition-all duration-300 hover:bg-white/20 dark:bg-white/5 dark:hover:bg-white/10 ${collapsed ? 'px-2' : 'gap-2 px-4'}`}
           >
             {theme === 'dark' ? (
               <>
                 <BiSun className="text-lg" />
-                <span className="text-sm">切换到浅色</span>
+                <span className={collapsed ? 'sr-only' : 'text-sm'}>切换到浅色</span>
               </>
             ) : (
               <>
                 <BiMoon className="text-lg" />
-                <span className="text-sm">切换到深色</span>
+                <span className={collapsed ? 'sr-only' : 'text-sm'}>切换到深色</span>
               </>
             )}
           </motion.button>
         )}
+
+        <motion.button
+          type="button"
+          initial={{ opacity: 0 }}
+          animate={{ opacity: 1 }}
+          transition={{ delay: 0.5, duration: 0.3 }}
+          onClick={() => onCollapsedChange(!collapsed)}
+          aria-expanded={!collapsed}
+          aria-controls="sidebar-navigation"
+          aria-label={collapsed ? '展开侧边栏' : '折叠侧边栏'}
+          title={collapsed ? '展开侧边栏' : '折叠侧边栏'}
+          className={`mt-3 flex w-full items-center justify-center rounded-lg bg-white/10 py-2.5 text-white transition-all duration-300 hover:bg-white/20 ${collapsed ? 'px-2' : 'gap-2 px-4'}`}
+        >
+          {collapsed ? <BiChevronRight className="text-xl" /> : <BiChevronLeft className="text-xl" />}
+          <span className={collapsed ? 'sr-only' : 'text-sm'}>
+            {collapsed ? '展开侧边栏' : '折叠侧边栏'}
+          </span>
+        </motion.button>
       </motion.div>
 
       {/* 导航菜单 */}
-      <nav className="p-4 space-y-4 overflow-y-auto h-[calc(100vh-280px)]">
+      <nav id="sidebar-navigation" className="min-h-0 flex-1 space-y-4 overflow-y-auto p-4">
         {/* 环境配置分组 */}
         <div>
-          <div className="px-4 py-2 text-white dark:text-waterloo text-xs uppercase tracking-wide">环境配置</div>
+          <div className={`px-4 py-2 text-xs uppercase tracking-wide text-white dark:text-waterloo ${collapsed ? 'sr-only' : ''}`}>环境配置</div>
           <div className="space-y-2">
             {envGroup.map((item, index) => {
               const Icon = item.icon
@@ -163,8 +187,10 @@ export default function Sidebar() {
                 >
                   <Link
                     href={item.href}
+                    title={collapsed ? item.label : undefined}
                     className={`
-                      group flex items-center gap-3 px-4 py-3 rounded-xl transition-all duration-300
+                      group relative flex items-center rounded-xl py-3 transition-all duration-300
+                      ${collapsed ? 'justify-center px-2' : 'gap-3 px-4'}
                       ${isActive
                         ? 'bg-white/25 dark:bg-white/5 text-white shadow-solid-3 backdrop-blur-sm border-l-4 border-cyan-300'
                         : 'text-white dark:text-manatee hover:bg-white/15 dark:hover:bg-white/5 hover:translate-x-1'
@@ -172,9 +198,9 @@ export default function Sidebar() {
                     `}
                   >
                     <Icon className={`text-xl ${isActive ? 'text-cyan-300' : item.color} group-hover:scale-110 transition-transform`} />
-                    <span className="font-medium">{item.label}</span>
+                    <span className={collapsed ? 'sr-only' : 'font-medium'}>{item.label}</span>
                     {isActive && (
-                      <div className="ml-auto">
+                      <div className={collapsed ? 'absolute right-1 top-1/2 -translate-y-1/2' : 'ml-auto'}>
                         <div className="w-2 h-2 bg-cyan-300 rounded-full animate-pulse"></div>
                       </div>
                     )}
@@ -187,7 +213,7 @@ export default function Sidebar() {
 
         {/* 平台配置分组 */}
         <div>
-          <div className="px-4 py-2 text-white dark:text-waterloo text-xs uppercase tracking-wide">平台配置</div>
+          <div className={`px-4 py-2 text-xs uppercase tracking-wide text-white dark:text-waterloo ${collapsed ? 'sr-only' : ''}`}>平台配置</div>
           <div className="space-y-2">
             {platformGroup.map((item, index) => {
               const Icon = item.icon
@@ -201,8 +227,10 @@ export default function Sidebar() {
                 >
                   <Link
                     href={item.href}
+                    title={collapsed ? item.label : undefined}
                     className={`
-                      group flex items-center gap-3 px-4 py-3 rounded-xl transition-all duration-300
+                      group relative flex items-center rounded-xl py-3 transition-all duration-300
+                      ${collapsed ? 'justify-center px-2' : 'gap-3 px-4'}
                       ${isActive
                         ? 'bg-white/25 dark:bg-white/5 text-white shadow-solid-3 backdrop-blur-sm border-l-4 border-cyan-300'
                         : 'text-white dark:text-manatee hover:bg-white/15 dark:hover:bg-white/5 hover:translate-x-1'
@@ -210,9 +238,9 @@ export default function Sidebar() {
                     `}
                   >
                     <Icon className={`text-xl ${isActive ? 'text-cyan-300' : item.color} group-hover:scale-110 transition-transform`} />
-                    <span className="font-medium">{item.label}</span>
+                    <span className={collapsed ? 'sr-only' : 'font-medium'}>{item.label}</span>
                     {isActive && (
-                      <div className="ml-auto">
+                      <div className={collapsed ? 'absolute right-1 top-1/2 -translate-y-1/2' : 'ml-auto'}>
                         <div className="w-2 h-2 bg-cyan-300 rounded-full animate-pulse"></div>
                       </div>
                     )}
@@ -229,11 +257,11 @@ export default function Sidebar() {
         initial={{ y: 20, opacity: 0 }}
         animate={{ y: 0, opacity: 1 }}
         transition={{ delay: 0.8, duration: 0.5 }}
-        className="absolute bottom-0 left-0 right-0 p-4 border-t border-white/10 dark:border-strokedark"
+        className="shrink-0 border-t border-white/10 p-4 dark:border-strokedark"
       >
         {/* 版本信息 */}
         <div className="text-center">
-          <p className="text-white/60 dark:text-waterloo text-xs">v1.0.0</p>
+          <p className={`text-xs text-white/60 dark:text-waterloo ${collapsed ? 'sr-only' : ''}`}>v1.0.0</p>
         </div>
       </motion.div>
     </motion.div>
diff --git a/front/app/layout.tsx b/front/app/layout.tsx
index 734af32..d8fed23 100644
--- a/front/app/layout.tsx
+++ b/front/app/layout.tsx
@@ -1,16 +1,45 @@
 "use client";
 
-import type { Metadata } from "next";
+import { useEffect, useState } from "react";
 import "./globals.css";
 import Sidebar from "./components/Sidebar";
 import ContentArea from "./components/ContentArea";
 import { ThemeProvider } from "next-themes";
 
+const SIDEBAR_COLLAPSED_STORAGE_KEY = "get-jobs-sidebar-collapsed";
+
 export default function RootLayout({
   children,
 }: Readonly<{
   children: React.ReactNode;
 }>) {
+  const [sidebarCollapsed, setSidebarCollapsed] = useState(false);
+  const [sidebarPreferenceLoaded, setSidebarPreferenceLoaded] = useState(false);
+
+  useEffect(() => {
+    try {
+      const stored = window.localStorage.getItem(SIDEBAR_COLLAPSED_STORAGE_KEY);
+      setSidebarCollapsed(stored === "true");
+    } catch {
+      // 本地存储不可用时使用默认展开状态。
+    } finally {
+      setSidebarPreferenceLoaded(true);
+    }
+  }, []);
+
+  useEffect(() => {
+    if (!sidebarPreferenceLoaded) return;
+
+    try {
+      window.localStorage.setItem(
+        SIDEBAR_COLLAPSED_STORAGE_KEY,
+        String(sidebarCollapsed),
+      );
+    } catch {
+      // 本地存储不可用时仍保留当前页面内的折叠状态。
+    }
+  }, [sidebarCollapsed, sidebarPreferenceLoaded]);
+
   return (
     <html lang="zh-CN" suppressHydrationWarning>
       <head>
@@ -29,8 +58,11 @@ export default function RootLayout({
           enableSystem={false}
         >
           <div className="flex min-h-screen">
-            <Sidebar />
-            <ContentArea>
+            <Sidebar
+              collapsed={sidebarCollapsed}
+              onCollapsedChange={setSidebarCollapsed}
+            />
+            <ContentArea sidebarCollapsed={sidebarCollapsed}>
               {children}
             </ContentArea>
           </div>
diff --git "a/.codestable/features/2026-08-06-sidebar-collapse/baseline/\350\277\233\345\272\246\347\231\275\346\235\277.md" "b/\350\277\233\345\272\246\347\231\275\346\235\277.md"
index fbe32b9..c2f91b8 100644
--- "a/.codestable/features/2026-08-06-sidebar-collapse/baseline/\350\277\233\345\272\246\347\231\275\346\235\277.md"
+++ "b/\350\277\233\345\272\246\347\231\275\346\235\277.md"
@@ -35,6 +35,7 @@
 ✅ 已完成：51job AI逐岗位打招呼 —— 默认关闭，开启后逐岗位读取JD、生成AI文案并自动发送，无效文案或缺少JD时跳过。
 ✅ 已完成：51job AI参数与状态 —— 页面支持开关、单次上限和随机间隔，分析列表显示打招呼状态，运行接口回传计数。
 ✅ 已完成：51job AI本轮验证与产物 —— Java测试、页面定向lint、生产构建、diff检查、patch校验和回滚脚本语法检查均已完成。
+✅ 已完成：侧边栏可折叠 —— 支持展开/80px图标栏切换，内容区同步扩展，刷新和路由切换后保留状态。
 🔨 正在做：无。
 ⬜ 还没做：真实环境复测跳页 URL 是否稳定（若 currentPage 直跳失败再降级连点翻页）。
 ❓ 需要你决定：无。
