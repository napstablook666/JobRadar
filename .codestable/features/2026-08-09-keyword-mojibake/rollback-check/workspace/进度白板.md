✅ 已完成：智联招聘配置 —— 已保存 5 个医疗器械相关关键词、城市“不限”、薪资“6k以上”。
✅ 已完成：猎聘岗位筛选配置 —— 已保存 18 个放疗/医疗设备相关关键词、城市“全国”、薪资“6k-10k”。
✅ 已完成：AI配置更新 —— 已根据张涵简历补充详细技能介绍和放疗设备方向提示词，保留 5 个动态占位符。
✅ 已完成：AI生成求职文本测试 —— AI配置页可用当前编辑内容生成 HR 打招呼语，不保存配置、不启动投递。
✅ 已完成：猎聘投递卡死修复 —— 搜索响应改为同步捕获，初始化和投递不再并发访问共享 Playwright。
✅ 已完成：猎聘分页控件兼容 —— 单页结果没有分页时仍能处理岗位，存在分页时继续翻页。
✅ 已完成：猎聘投递状态反馈 —— 页面轮询任务状态，能显示启动、进度、成功和失败消息。
✅ 已完成：猎聘薪资本地筛选重构 —— 不再使用网页年薪档位；年薪统一换算为月薪 K，岗位与期望范围有交集即进入后续筛选。
✅ 已完成：猎聘逐岗位 AI 招呼语 —— 读取 JD、生成 AI 话术、展示岗位确认卡，用户确认后在聊天窗口发送。
✅ 已完成：猎聘发送结果闸门 —— 仅聊天窗口新增本人消息标记已投递，继续聊和发送不确定均不冒充成功。
✅ 已完成：猎聘投递参数与停止控制 —— 支持 AI 开关、单次上限、随机间隔，停止任务会取消待确认并打断等待。
✅ 已完成：AI不匹配自动发送平台默认沟通语 —— AI 返回 false 后自动打开聊天并确认平台默认语已发出。
✅ 已完成：猎聘实时发送确认 —— 以聊天窗口新增的本人消息确认成功，并兼容抽屉式聊天关闭按钮。
✅ 已完成：本地回归验证 —— Java 全量测试、前端 ESLint、Next.js 生产构建均已执行。
✅ 已完成：真实投递复测 —— false 分支成功发送 1 条平台默认沟通语后已停止任务。
✅ 已完成：真实 AI 话术复测 —— 本轮确认可用 AI 话术，聊天窗口实际出现 AI 招呼语和简历卡片，卡片包含“在线简历/附件简历”，岗位状态更新为已投递。
✅ 已完成：AI话术自动发简历 —— AI话术确认成功后自动点击“发简历”和“立即投递”，并以聊天新增本人简历消息作为成功依据。
✅ 已完成：猎聘 AI 投递相关度闸门 —— AI 开启时，缺 JD、无关岗位、AI false、空话术和请求异常均在点击聊天前跳过，不再发送平台预设语。
✅ 已完成：猎聘分析默认口径 —— 分析页首次请求不带状态筛选并显示完整岗位池，手动应用状态后按筛选结果统计。
✅ 已完成：本轮回归验证 —— Java 全量测试、前端生产构建和已投递统计接口验证通过。
✅ 已完成：猎聘访问节奏闸门 —— 搜索、翻页、详情和发送共用账户级间隔，连续 5 次成功发送后自动冷却。
✅ 已完成：猎聘异常停止 —— 搜索响应 403/429 时停止任务并记录状态，旧的 2-5 秒配置运行时收敛到 90-180 秒。
✅ 已完成：本轮验证与回滚产物 —— Java 全量测试、前端 ESLint、生产构建、补丁应用检查和反向回滚脚本均已准备。
✅ 已完成：猎聘投递页码断点续跑 —— 按关键词+城市+薪资记住已完成页，下次从下一页接着投；配置页可查看进度并一键重置。
✅ 已完成：投递页面生命周期恢复 —— 页面关闭时自动恢复并重试一次，发送后异常显示结果不确定，避免重复投递。
✅ 已完成：BAT 启动脚本适配 —— 从任意工作目录一键启动前端 6866 和后端 8888，支持 JDK 21、中文空格路径、日志和参数转发。
✅ 已完成：BAT 启停验证 —— 启动返回 0、前端 HTTP 200、后端端口就绪，停止后 6866/8888 端口释放。
✅ 已完成：51job批量投递确认兼容 —— 识别当前页面的单一批量按钮和无数字“投递成功”弹窗，真实确认并更新 20 条已投递记录。
✅ 已完成：51job运行复测 —— 启动接口、成功计数、停止接口和最终任务收尾均已实测通过。
✅ 已完成：猎聘零成功计数修复 —— 只有聊天确认成功才计入投递数，零成功时显示“本轮未成功发起聊天”，并补充回归测试与回滚产物。
✅ 已完成：猎聘任务轮次与筛选汇总 —— 暂停后重新开始隔离旧状态，页面显示本轮处理数、薪资放行/跳过数和成功聊天数。
✅ 已完成：清理未投递岗位统计记录 —— 删除 3397 条未投递历史记录，保留 21 条已投递记录，统计接口返回 pending=0。
✅ 已完成：猎聘 AI 筛选自动投递 —— 新增默认关闭的评分自动投递开关、最低评分配置和严格 JSON 评分闸门，保留人工确认模式。
✅ 已完成：猎聘 AI 批量评分投递提速 —— 增加批量评分/话术、命名模板、评分与话术缓存、BATCH_SHADOW 预演和 BATCH_AUTO 自动投递，按岗位 ID 严格回填。
✅ 已完成：猎聘批量方案验证 —— Java 100 个测试、猎聘与 AI 配置页定向 lint、Next.js 13 页生产构建和回滚预览通过；运行态已有任务保持不变。
✅ 已完成：猎聘实时状态日志 —— 页面保留最近 8 条筛选、跳过、评分和投递消息，最新状态自动跟随显示。
✅ 已完成：投递间歇提示时序修复 —— 等待开始、等待结束和无需额外等待分别展示，页面提示与真实节奏同步。
✅ 已完成：51job AI逐岗位打招呼 —— 默认关闭，开启后逐岗位读取JD、生成AI文案并自动发送，无效文案或缺少JD时跳过。
✅ 已完成：51job AI参数与状态 —— 页面支持开关、单次上限和随机间隔，分析列表显示打招呼状态，运行接口回传计数。
✅ 已完成：51job AI本轮验证与产物 —— Java测试、页面定向lint、生产构建、diff检查、patch校验和回滚脚本语法检查均已完成。
✅ 已完成：侧边栏可折叠 —— 支持展开/80px图标栏切换，内容区同步扩展，刷新和路由切换后保留状态。
✅ 已完成：APP 风控节奏适配 —— 搜索、翻页、详情、发送使用分层随机间隔，连续发送按批次进入可配置冷却。
✅ 已完成：APP 风控信号停止 —— 识别 403/429、频繁访问、安全验证、验证码和行为异常等信号并停止任务。
✅ 已完成：APP 风控调研与参数结论 —— 公开材料未给出固定秒数，当前值作为保守工程起点并记录校准限制。
✅ 已完成：本轮验证与回滚产物 —— Java 全量测试、前端 ESLint、Next.js 生产构建、patch 应用和隔离回滚均通过。
✅ 已完成：四个平台关键词逐行输入与统一筛选配置 —— 四个平台均显示每行一个关键词，已写入18个放疗/医疗设备关键词、城市不限和6k-10k对应筛选。
✅ 已完成：猎聘一键清除未投递岗位 —— 分析页读取全局待清理数量，确认后事务清除 `delivered=0/NULL`，保留已投递岗位、分页进度与 AI 缓存。
✅ 已完成：猎聘清理接口隔离回归 —— 临时新编译后端的摘要、统计、列表和分页进度一致；未调用真实删除接口。
✅ 已完成：APP 间歇参数提速 —— 搜索、翻页、详情、发送和批次冷却统一切换到更短随机区间，下一轮任务生效。
✅ 已完成：APP本轮统计实时显示 —— 任务运行中实时回传岗位筛选和 AI 处理计数，结束后保留本轮结果。
✅ 已完成：本轮统计回归验证 —— Java 全量测试、页面 lint 和 Next.js 生产构建均通过。
✅ 已完成：猎聘停止投递即时收尾 —— 岗位卡片、聊天回执、发简历弹窗和输入框等待均响应停止信号，页面显示“停止中”。
✅ 已完成：主端口服务重载与停止接口回归 —— `6866/8888` 已加载最新源码，启动后立即停止在 288ms 内收敛到 `PAUSED`，本轮成功数为 0。
✅ 已完成：APP 极限激进间歇 —— 默认发送 30-60 秒、搜索 10-20 秒、翻页 5-10 秒、详情 8-15 秒、批次冷却 300-450 秒。
✅ 已完成：APP 极限间歇验证与回滚 —— Java、页面构建、数据库、源码 patch 和隔离回滚均已通过。
✅ 已完成：猎聘批量发送引用重定位 —— AI 等待结束后按岗位 ID 重新定位当前卡片和聊一聊按钮，卡片索引仅作回退。
✅ 已完成：猎聘批量失败页码闸门 —— 按钮失效、点击异常或聊天确认失败均保留当前页，不再保存虚假的完成进度。
✅ 已完成：AI失败独立统计 —— 状态接口和页面分别显示 AI 超时、结果无效和按钮失效计数。
✅ 已完成：本轮批量投递回归验证 —— 新增无 Locator 引用、失败重试和超时识别测试，定向测试通过。
🔨 正在做：新档位运行态重载 —— 源码和数据库已落盘，待服务重载后启动下一轮任务。
⬜ 还没做：新一轮 APP 真实复测。
⬜ 还没做：真实环境复测跳页 URL 是否稳定（若 currentPage 直跳失败再降级连点翻页）。
✅ 已完成：猎聘批量发送真实复测 —— 重载修复后的后端后启动 runId=1，处理42个岗位，AI通过1个并确认成功投递1个；原单次上限已恢复为10。
❓ 需要你决定：无。

✅ 已完成：APP AI 全自动投递真实实例复测 —— visible-login 隔离运行 `runId=1` 自然完成，扫描131个岗位，AI通过5个、复核8个，成功发起1个聊天。
✅ 已完成：APP 发送确认链路验证 —— AI话术与简历发送链路完成，`buttonFailures=0`、`confirmationFailures=0`、`aiTimeouts=0`；收尾后配置恢复 `maxPerRun=10`、`BATCH_AUTO`、AI最低分70。
✅ 已完成：本轮记录收口 —— 全量 Java 测试、`git diff --check`、回滚脚本双向回读通过；本地提交 `136c387`。
🔨 正在做：无 —— 实例测试、配置回读、健康检查和回滚记录已完成。
⬜ 还没做：无。
❓ 需要你决定：无。

✅ 已完成：猎聘 AI 超时自动重试 —— 评分和话术请求仅对超时追加可配置重试，默认追加 3 次并递增等待。
✅ 已完成：猎聘 AI 超时手动重试 —— 超时耗尽的岗位保留当前页，页面显示待重试数量并提供手动重试按钮。
✅ 已完成：猎聘 AI 重试回归 —— Java 全量测试、猎聘页 ESLint、Next.js 生产构建和 diff 检查通过。
✅ 已完成：猎聘 AI 超时一键批量重试 —— 新增单次点击的连续重试循环，待重试数量下降就继续，归零完成或无进展暂停。
✅ 已完成：猎聘 AI 批量重试状态展示 —— 页面显示批量轮次、剩余数量、累计成功聊天数，停止按钮可中断轮间等待。
✅ 已完成：猎聘 AI 批量重试验证与回滚 —— 定向/全量 Java、页面 ESLint、生产构建、diff 检查和隔离回滚均通过。
🔨 正在做：运行态加载 —— 源码和验证产物已落盘，8888 当前保持现有运行态，等待合适窗口加载新后端。
⬜ 还没做：新版本重载后的 AI 批量重试真实复测。
❓ 需要你决定：当前运行任务结束后是否立即重载后端并做真实批量复测。

✅ 已完成：51job启动反馈与实时运行记录 —— 点击开始投递后立即显示状态，页面实时保留最新5条运行消息，任务结束自动恢复按钮。
🔨 正在做：无 —— 51job本轮代码、定向测试和运行态验证已完成。
⬜ 还没做：全量 Java 回归 —— 当前被工作区原有猎聘代码编译错误阻塞。
❓ 需要你决定：无。

✅ 已完成：51job共享浏览器锁可取消等待 —— 其他平台占用浏览器时显示等待，停止请求可取消排队并恢复按钮。
🔨 正在做：无 —— 定向测试、全量 Java 测试、页面 ESLint、真实启停和隔离回滚均已完成。
⬜ 还没做：无。
❓ 需要你决定：无。

✅ 已完成：猎聘投递页码停止/关闭自动保存 —— 停止脚本和应用正常关闭先等待任务收尾，半页中断下次从当前页重跑，终态页面即时显示已保存断点。
🔨 正在做：无 —— 后端停止等待、脚本优雅关闭、前端进度刷新和回归测试已完成。
⬜ 还没做：无。
❓ 需要你决定：无。

✅ 已完成：猎聘筛选后暂缓岗位批量重试 —— 一键批量重试只处理已通过前置筛选、因 AI 请求或发送网络问题暂缓的岗位；普通未投递历史不再进入该任务。
✅ 已完成：猎聘暂缓队列隔离验证 —— AI 暂缓 1 条、网络暂缓 1 条可被接口统计，另有 2121 条普通未投递未进入队列。
✅ 已完成：猎聘清理未投递运行态修复 —— 重载 8888 后暂缓统计恢复，实际清除 2120 条未投递记录，保留 30 条已投递记录。
✅ 已完成：前端重构方案确认 —— 已确认黑曜石作战舱、性能优先渲染与全量页面覆盖。
✅ 已完成：前端控制台重构 —— 全站统一为 Lucide 作战工作台，配置/分析路由、深浅主题和懒加载图表均可用。
✅ 已完成：前端成品验收 —— lint、生产构建、1440px/375px、抽屉关闭和缩减动效均已实测通过。
🔨 正在做：无 —— 本轮前端重构已完成。
⬜ 还没做：无。
❓ 需要你决定：无。

✅ 已完成：Code Humanizer 结构审查 —— 已完成当前源码改动的重复实现、死代码、异常吞咽和调试噪音扫描，形成中文报告。
🔨 正在做：无 —— 当前停在报告模式，源码清理待单独进入 Fix 模式。
⬜ 还没做：按报告严重度逐批处理共享页头、平台恢复流程、分析页重复流程和空异常处理；为前端共享逻辑补充表征测试。
❓ 需要你决定：是否进入 Fix 模式处理报告中的高风险与低风险项。

✅ 已完成：配置/分析导航去重 —— 四个平台配置页删除底部重复按钮和内嵌分析面板，顶部入口保持不变。
✅ 已完成：导航变更验证与回滚产物 —— lint、生产构建、路由检查、截图检查、补丁校验和隔离回滚均已记录。
🔨 正在做：无 —— 本轮实现与验证产物已完成。
⬜ 还没做：无。
❓ 需要你决定：无。

✅ 已完成：AI招呼语固定开头与姓名过滤 —— AI 生成文本直接以“您好，我是应用物理学应届毕业生，”开头，正文不带候选人姓名。
✅ 已完成：AI话术统一校验 —— 预览、APP、51job 和批量入口统一拦截错误前缀、命名表达、Markdown、多行、超长和 `false` 结果。
✅ 已完成：AI招呼语运行态与回滚 —— 目标配置已恢复并回读一致，基线回滚脚本、源码补丁和隔离反向回滚均已验证。
🔨 正在做：无 —— Java 全量测试、前端 ESLint、生产构建、真实预览和服务状态检查均已完成。
⬜ 还没做：无。
❓ 需要你决定：无。

✅ 已完成：51job JD 与 AI 稳定性修复 —— 搜索快照、详情页 JD 回退和 AI 回复解析均支持多种实际响应形态，失败状态带具体原因。
🔨 正在做：无 —— 源码、测试、构建、补丁回放和隔离回滚均已完成。
⬜ 还没做：真实运行态小批量复测，确认当前登录态下详情页和 AI 兼容层的最终响应。
❓ 需要你决定：无。

✅ 已完成：浏览器后台模式改造 —— 默认使用无界面浏览器，导航和新页面保持后台运行，管理页改为手动打开。
✅ 已完成：后台登录边界 —— 后台模式仅读取登录状态，扫码入口统一要求 visible-login 模式。
✅ 已完成：后台模式验证 —— Java 全量测试、PowerShell 脚本解析和目标文件 diff 检查均通过。
🔨 正在做：运行态重载 —— 当前投递任务仍在执行，等待自然收尾后再加载后台模式。
⬜ 还没做：重载后的真实无界面进程检查；visible-login 扫码回归。
❓ 需要你决定：无，任务收尾后按默认 background 模式重启。

✅ 已完成：APP 网页自定义薪资联动 —— 启动投递时先回到第一页，按月薪配置换算成年薪万元，自动完成“薪资 → 自定义 → 确定”并以确认响应作为岗位数据。
✅ 已完成：APP 薪资断点续跑适配 —— 已设置网页薪资后通过分页控件回到断点页，本地月薪交集闸门继续保留。
✅ 已完成：网页薪资行为回归 —— 实测自定义输入与确认请求，页面请求带 `salaryCode=7$12`；平台将 7.2 万下限归一为 7 万，未收窄本地 6K-10K 放行范围。
✅ 已完成：本轮代码验证 —— Java 全量测试、前端 ESLint、Next.js 生产构建均通过。
🔨 正在做：无 —— 本轮实现与验证已完成。
⬜ 还没做：真实启动投递后的完整运行态复测。
❓ 需要你决定：无。

✅ 已完成：BAT 运维入口三件套 —— 根目录统一保留 `start.bat`、`restart.bat`、`stop.bat`，启动、重启、停止入口清晰分开。
✅ 已完成：BAT 重启链路隔离验证 —— 停止顺序、参数透传、失败码传播、路径解析和回滚均已验证。
🔨 正在做：运行态重启收口 —— 真实重启后前端已恢复，后端标准 bootRun 受既有 Java 编译错误影响，当前已用旧编译产物恢复 8888。
⬜ 还没做：既有 Java 编译错误修复后，用标准 `restart.bat` 再做一次后端就绪验证。
❓ 需要你决定：无。

✅ 已完成：猎聘混合读取 —— 默认 hybrid 先用 Java HttpClient 读取岗位 JSON，页面继续负责 DOM 和投递，HTTP 异常自动回退 Playwright。
✅ 已完成：统一读路径状态 —— 猎聘与 51job 状态接口都返回 route、fallback、detail，便于运行态判断实际读取链路。
✅ 已完成：混合读取回归与回滚产物 —— 169 个 Java 测试通过，补丁隔离回放和反向回放哈希一致，验证记录与回滚脚本已落盘。
🔨 正在做：无 —— 本轮代码、测试和回滚产物已完成。
✅ 已完成：混合读取真实端点小批量复测 —— 真实任务最终走浏览器搜索响应并完成 1 次聊天、AI 话术和简历发送。

✅ 已完成：猎聘搜索响应超时修复 —— 搜索、薪资确认和翻页统一使用有上限的响应等待，并在浏览器响应未捕获时回退 HTTP 读取。
✅ 已完成：猎聘网页年薪整数归一 —— 自定义薪资按覆盖范围向外取整，避免小数输入被页面控件清空。
✅ 已完成：猎聘真实投递复测 —— `runId=1` 完成 82 个岗位扫描，AI 通过 1 个并确认聊天、AI 话术和简历均已发送 1 次。
✅ 已完成：搜索超时补丁验证与回滚 —— 全量 Java 测试、隔离补丁回放、三份文件哈希比对和隔离回滚均通过。
🔨 正在做：无 —— 配置已恢复原值 `maxPerRun=10`，服务保持运行。
⬜ 还没做：无。
❓ 需要你决定：无。

✅ 已完成：四平台并行投递实现 —— 四个平台先原子占位，再进入投递专用线程池，可同时运行各自的平台任务。
✅ 已完成：独立后台浏览器运行时 —— 默认 isolated + background，每个平台独立 Playwright 资源；扫码入口保留 visible-login。
✅ 已完成：并行投递回归与交付产物 —— Java 定向/全量测试、diff 检查、patch 反向检查和隔离回滚哈希比对均通过。
🔨 正在做：运行态加载 —— 源码已编译验证，当前 8888 仍是旧后端实例，尚未打断现有服务重载。
⬜ 还没做：重载新后端后检查 `/api/playwright/status` 的 `deliveryRuntime`、四个平台运行时状态和后台无窗口进程；需要扫码时再做 visible-login 回归。
❓ 需要你决定：无；下一次允许服务重载时按已确认配置加载。

✅ 已完成：猎聘双任务槽并行 —— 普通投递与筛选后暂缓岗位批量重试各自维护运行、停止、确认、统计、日志和待处理岗位状态，可同时启动。
✅ 已完成：猎聘双槽前端控制 —— 配置页显示暂缓任务的独立启动/停止、轮次、剩余数、日志和确认卡；分析页在普通投递运行时保留暂缓重试入口，清理动作检查两个任务槽。
✅ 已完成：双槽状态契约收敛 —— `/api/liepin/status` 保留普通投递旧顶层字段，并提供 `normalTask`、`suspendedRetryTask`、`isAnyRunning`；旧实例响应仍可由前端兼容读取。
✅ 已完成：本轮代码验证 —— 前端 `pnpm lint`、`pnpm build`、Java 猎聘定向测试和 `git diff --check` 均通过。
🔨 正在做：运行态加载 —— 当前 8888 普通投递仍在执行，源码新版本等待自然收尾后再重载。
⬜ 还没做：新后端重载后的双任务真实并行小批复测。
❓ 需要你决定：无；重载前保持当前运行任务和端口状态。

✅ 已完成：放射技师岗位关键词配置 —— 已根据简历将猎聘关键词替换为18条，每行一个，覆盖放射/放疗技师、医学影像和放疗设备应用方向。
🔨 正在做：无 —— 配置写入、回读、回滚和目标恢复均已完成。
⬜ 还没做：新关键词下启动新的投递复测。
❓ 需要你决定：是否启动新关键词投递复测。

✅ 已完成：猎聘本轮自然结束验证 —— `runId=2` 正常进入 `COMPLETED`，处理630个岗位，成功发起10个聊天并达到单次上限。
✅ 已完成：猎聘真实发送链路复核 —— AI通过11个，实际发送5次AI话术和简历的批次调用，最终成功10条；`buttonFailures=0`、`confirmationFailures=0`、`aiTimeouts=0`。
✅ 已完成：人工确认分支判定 —— 终态存在真实成功投递，不切换 `MANUAL`，原 `BATCH_AUTO`、AI最低分70和单次上限10配置保持不变。
🔨 正在做：无 —— 本轮运行与验证已完成。
⬜ 还没做：无。
❓ 需要你决定：无。

✅ 已完成：城市编码乱码归一化 —— APP 配置读取和保存统一兼容城市名、城市码与历史 UTF-8 乱码，`410` 可稳定回读为 `全国`。
✅ 已完成：城市配置回归与运行态加载 —— Java/前端检查通过，后端重载后未知城市返回 400，原配置已恢复。
🔨 正在做：无 —— 本轮源码、测试、补丁、验证记录和回滚脚本已完成。
⬜ 还没做：登录态下真实小批投递复测。
❓ 需要你决定：无。

✅ 已完成：四平台 Cookie 登录统一 —— Boss、猎聘、51job、智联配置页均使用同一导入卡，导入后即时回显登录检测结果并自动持久化到本机。
✅ 已完成：Cookie 登录前端验证 —— 四页定向 ESLint 与 Next.js 生产构建通过，旧扫码后端接口保留但配置页不再展示。
🔨 正在做：无 —— 等待在各平台粘贴有效 Cookie 后做实际登录态复测。
⬜ 还没做：四个平台 Cookie 真实导入与登录态恢复复测。
❓ 需要你决定：无。

✅ 已完成：Cookie 本地持久化状态与失效提示 —— 后端仅返回是否已保存和是否需要重导入；页面发现本地 Cookie 未通过登录检测时，明确提示重新登录浏览器并导入完整 Cookie。
✅ 已完成：Cookie 状态接口与构建验证 —— `GET /api/cookie/status` 不回传 Cookie 内容，后端编译、前端定向 lint 和生产构建均通过。
🔨 正在做：无 —— 当前猎聘本机 Cookie 已保存，但运行时仍未检测到登录态。
⬜ 还没做：重新获取猎聘完整 Cookie 后的真实导入复测。
❓ 需要你决定：无。

✅ 已完成：四平台 Cookie 双入口 —— 配置页统一提供可见浏览器登录和手动 Cookie 填写，浏览器登录成功后自动保存并回写登录状态。
✅ 已完成：项目浏览器生命周期清理 —— 四个平台停止投递会在任务收尾后关闭应用浏览器，`stop.bat` 会优雅停止任务并清理项目 Chromium profile，不影响普通 Chrome。
✅ 已完成：本轮验证 —— Java 全量测试、前端定向 ESLint、Next.js 生产构建、浏览器登录接口、四平台状态接口和 `stop.bat` 端口/进程清理均通过。
🔨 正在做：无 —— 服务重载和功能验证已完成。
⬜ 还没做：各平台实际重新登录后的真实 Cookie 恢复复测。
❓ 需要你决定：无。

✅ 已完成：Cookie 手动获取收口 —— 四个平台浏览器登录改为用户点击“获取并保存 Cookie”后才读取、落库和注入；状态轮询只展示登录状态。
✅ 已完成：自动保存入口清理 —— 配置保存、SSE 登录事件和后台登录检测不再自动写 Cookie，手动导入入口保持可用。
✅ 已完成：捕获接口验证 —— `/api/cookie/browser/capture` 已上线，四个平台未打开浏览器时均返回明确提示且不泄露 Cookie 内容。
✅ 已完成：猎聘登录误判修复 —— 收窄登录入口选择器，并用 `lt_auth` 与 `UniqueKey/user_name` 组合兜底，已登录页面不再被无关登录链接误判。
✅ 已完成：猎聘实例登录与捕获复测 —— 重启服务后从页面点击打开浏览器并获取 Cookie，返回 `captured=true`、`loggedIn=true`、30 条 Cookie，`needsReimport=false`。
🔨 正在做：无 —— Java 测试、前端 lint/build 和服务重载验证均完成。
⬜ 还没做：其它三个平台重新登录后的真实 Cookie 恢复复测。
❓ 需要你决定：无。

✅ 已完成：Cookie 捕获登录态收敛 —— 捕获回调使用导入后的真实检测结果，避免登录跳转瞬间误报未登录。
✅ 已完成：四平台登录检测去误报 —— 公共登录/安全验证页面不再因缺少旧选择器被判为已登录，未知状态保持未登录。
✅ 已完成：Cookie 状态回写页面 —— 刷新配置页时使用 `/api/cookie/status` 的登录结果，避免旧 SSE 值覆盖本机有效状态。
✅ 已完成：本轮代码验证 —— Java 测试、前端 lint、Next.js 生产构建均通过；按要求未追加实例复测。
🔨 正在做：无 —— 本轮 Cookie 登录修复已落盘。
⬜ 还没做：用户重新登录其它平台后再进行真实恢复复测。
❓ 需要你决定：无。

✅ 已完成：猎聘暂缓岗位搜索解析修复 —— 浏览器搜索响应支持多层包装、岗位字段变体、嵌套岗位 ID 和空列表，结构异常进入可重试暂停。
✅ 已完成：解析修复回归与回滚产物 —— Java 全量 190 个测试通过，定向解析测试、补丁回放和隔离回滚均通过。
🔨 正在做：无 —— 本轮源码、测试和 CodeStable 产物已完成。
⬜ 还没做：无。
❓ 需要你决定：无。

✅ 已完成：BAT 根入口可见化适配 —— 启动、停止、重启均显示执行目录、服务日志和退出结果，双击后保留可读反馈。
✅ 已完成：BAT 三入口 CRLF 与参数回归 —— 兼容中文空格路径，保留 `-NoBrowser` 等启动参数和被脚本调用时的无暂停模式。
✅ 已完成：猎聘搜索会话域隔离 —— HTTP 搜索只读取 `liepin.com` 的 Cookie、XSRF 与会话标识，避免独立运行时混入其他平台同名字段；定向回归通过。
✅ 已完成：猎聘发送前限速重定位修复 —— 发送限速等待结束后才按岗位 ID 重新定位当前卡片按钮，页面刷新不再让旧按钮引用失效。
✅ 已完成：猎聘单条真实投递复测 —— `runId=1` 处理 6 个岗位并确认聊天成功 1 条；`buttonFailures=0`、`confirmationFailures=0`，原 `maxPerRun=10` 与批量自动投递配置已恢复。
🔨 正在做：无 —— 本轮修复、定向回归和真实投递验证均已完成。
⬜ 还没做：无。
❓ 需要你决定：无。

✅ 已完成：猎聘静默投递回退 —— 定位到失效点为 `BATCH_AUTO` 自动分支，先暂停当前零成功运行轮次。
✅ 已完成：猎聘人工确认模式恢复 —— 投递模式已恢复为 `MANUAL`，保留 AI 招呼语、评分阈值和单次上限配置。
🔨 正在做：无 —— 配置持久化与任务终态已核对。
⬜ 还没做：无。
❓ 需要你决定：无。

✅ 已完成：Codex 项目级 Git 自动管理 —— 新增任务基线快照、只提交本次新改路径和反向提交回退规则。
✅ 已完成：人工确认回退稳定性验证 —— `MANUAL` 实例命中确认卡，未自动发送；三组猎聘定向 Java 测试通过。
🔨 正在做：无 —— 当前源码仍是混合工作树，尚未形成干净发布 commit。
⬜ 还没做：把现有历史改动分批归档成可读的本地提交。
❓ 需要你决定：是否按 Git 快照逐批整理历史改动。

✅ 已完成：自动 Git 管理方案调研 —— 使用 `smart-search-cli` 深度规划、Exa 官方页面抓取、GitHub API 元数据和 Codex 回滚议题交叉核对；重点比较 Entire CLI、agent-rollback、git-stint、autogit、ckpt、git-safepoint。
🔨 正在做：无 —— 当前结论是保留本地无推送护栏，先在隔离副本验证 Codex 专用快照层，再决定是否接入。
⬜ 还没做：隔离副本安装/回放候选工具；验证 Windows、918 条历史未跟踪项和现有 `codex-git.ps1` 的兼容性。
❓ 需要你决定：是否进入候选工具隔离试跑；默认建议先试 agent-rollback 的 dry-run/回滚，不启用自动 push。

✅ 已完成：APP 搜索契约真实复测 —— visible-login 隔离运行时 `runId=1` 处理 9 个岗位，5 条聊天成功，其中 4 条确认 AI 话术与简历均已发送；`buttonFailures=0`、`confirmationFailures=0`、`aiTimeouts=0`。
✅ 已完成：真实任务收尾与后台服务恢复 —— 普通任务和暂缓重试均已停止，服务回到 background/no-browser，健康接口 200，配置仍为 MANUAL、AI 开启、上限 10、薪资 6$10。
🔨 正在做：无 —— 代码、定向/全量测试和本轮实际投递均已完成。
⬜ 还没做：后台模式重新导入完整 APP Cookie 后的登录检测复测；当前后台接口仍返回 `needsReimport=true`，后续新任务前需在 visible-login 完成登录捕获。
❓ 需要你决定：无。
